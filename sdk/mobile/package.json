{
  "name": "@ice-truck/mobile-sdk",
  "version": "1.0.0",
  "description": "Mobile SDK for Ice Truck Tracking with offline cache",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "@react-native-async-storage/async-storage": "^1.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "peerDependencies": {
    "react-native": ">=0.60.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}